#ifndef OVERLAYLABEL_H
#define OVERLAYLABEL_H

#include <QLabel>

/**
 * @brief Виджет, отображающий полупрозрачный прямоугольник поверх изображения.
 *
 * Наследует QLabel и переопределяет paintEvent, чтобы поверх стандартного
 * содержимого (изображения) рисовать прямоугольник с заливкой и контуром.
 * Прямоугольник может использоваться для обозначения области интереса,
 * например, поля зрения спектрометра на обзорном снимке.
 *
 * @note Координаты прямоугольника задаются в системе координат самого виджета,
 *       а не исходного изображения. При масштабировании изображения
 * прямоугольник следует пересчитывать, если он определён в исходных
 * координатах.
 */
class OverlayLabel : public QLabel {
    Q_OBJECT
public:
    /**
     * @brief Конструктор.
     * @param parent Родительский виджет.
     *
     * По умолчанию прямоугольник отсутствует, заливка и контур имеют цвет
     * #d97706 (amber-600), прозрачность заливки 0.5.
     */
    explicit OverlayLabel(QWidget *parent = nullptr);

    /**
     * @brief Установить прямоугольник для отображения.
     * @param rect Прямоугольник в координатах виджета.
     *
     * Если прямоугольник недействителен (isNull()), он не отображается.
     * При установке нового прямоугольника виджет перерисовывается.
     */
    void setOverlayRect(const QRect &rect);

    /**
     * @brief Установить цвет прямоугольника.
     * @param color Цвет заливки и контура.
     *
     * Для заливки будет использоваться полупрозрачная версия этого цвета
     * (с учётом текущей прозрачности), для контура – чистый цвет.
     */
    void setOverlayColor(const QColor &color);

    /**
     * @brief Установить прозрачность заливки.
     * @param opacity Прозрачность от 0.0 (полностью прозрачный) до 1.0
     * (полностью непрозрачный).
     *
     * Не влияет на прозрачность контура.
     */
    void setOverlayOpacity(qreal opacity);

protected:
    /**
     * @brief Отрисовка содержимого.
     * @param event Событие перерисовки.
     *
     * Сначала вызывается стандартная отрисовка QLabel (изображение),
     * затем поверх рисуется прямоугольник с полупрозрачной заливкой и цветным
     * контуром.
     */
    void paintEvent(QPaintEvent *event) override;

private:
    QRect m_overlayRect;  //!< Прямоугольник в координатах виджета (если
                          //!< isNull(), не отображается)
    QColor m_overlayColor;  //!< Цвет заливки и контура
    qreal m_overlayOpacity;  //!< Прозрачность заливки (0.0–1.0)
};

#endif  // OVERLAYLABEL_H
